/**
 * Seed stub — sample categories/transactions in a later commit.
 */
async function main() {
  console.log("SpendWise seed: no-op stub (add sample data later).");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
